# Shape gasback variant + its full test suite (shares lib/ with the repo root).
[profile.default]
src = "."
test = "test"
out = "out"
libs = ["../../../lib"]
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"
